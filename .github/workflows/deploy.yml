name: Trigger Vercel Deploy
on:
  push:
    branches: [main]

permissions: {}

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Vercel
        run: curl -X POST ${{ secrets.VERCEL_DEPLOY_HOOK }}
